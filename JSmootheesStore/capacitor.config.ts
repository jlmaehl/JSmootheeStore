import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jsmoothee.app',
  appName: 'jsmoothees-store',
  webDir: 'dist/angularCapacitor',
  bundledWebRuntime: false
};

export default config;
